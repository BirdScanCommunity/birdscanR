% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/createDataPackage.R
\name{createDataPackage}
\alias{createDataPackage}
\title{Create MR1 Data Package}
\usage{
createDataPackage(
  echoData,
  protocolData,
  blindTimesData,
  sunriseSunsetData,
  radarSiteData,
  manualBlindTimes = NULL,
  dbName = NULL,
  pulseTypeSelection = NULL,
  rotationSelection = NULL,
  timeRangeTargetTZ = NULL,
  targetTimeZone = "Etc/GMT0",
  classSelection = NULL,
  classProbCutOff = NULL,
  altitudeRange_AGL = NULL,
  echoValidator = FALSE,
  outputDirPath = NULL,
  tagOutputFile = c(NULL, NULL),
  saveCSV = TRUE,
  saveAsRDS = FALSE
)
}
\arguments{
\item{echoData}{dataframe with the echo data from the data list created with
\code{\link[=extractDbData]{extractDbData()}}.}

\item{protocolData}{dataframe with the protocol data from the data list
created with \code{\link[=extractDbData]{extractDbData()}}. Echoes not detected during the listed
protocols will be excluded.}

\item{blindTimesData}{dataframe with the manual blind times created by
\code{\link[=loadManualBlindTimes]{loadManualBlindTimes()}}. It includes the automated blind times induced by
changes in measurement protocol, and blind times added manually to remove
periods of incoherent data collection.}

\item{sunriseSunsetData}{dataframe with sunrise/sunset, and civil and
nautical dawn/dusk. Computed with \code{\link[=twilight]{twilight()}}.}

\item{radarSiteData}{dataframe/vector with the database site table.}

\item{manualBlindTimes}{dataframe with the manual blind times created by the
function \code{\link[=loadManualBlindTimes]{loadManualBlindTimes()}}.}

\item{dbName}{Name of the database. Used as metadata and as part of the
auto-generated output filename.}

\item{pulseTypeSelection}{character vector with the pulse types which should
be included in the subset. Options: \code{S}, \code{M}, \code{L}, i.e. short-, medium-,
long-pulse, respectively. Default is \code{NULL}: no filtering applied based on
pulseType.}

\item{rotationSelection}{numeric vector to select the operation modes with
and/or without antenna rotation. Options: 0, 1. (0 = no rotation,
1 = rotation). Default is \code{NULL}: no filtering applied based on rotation mode.}

\item{timeRangeTargetTZ}{Character vector of length 2, with start and end of
time range, formatted as \code{"\%Y-\%m-\%d \%H:\%M"}. Echoes outside the time range
will be excluded.}

\item{targetTimeZone}{String specifying the target time zone.
Default is \code{"Etc/GMT0"}.}

\item{classSelection}{character string vector with the classes that should be
included. Default is \code{NULL}: all classes are included.}

\item{classProbCutOff}{numeric cutoff value for class probabilities. Echoes
with a lower class probability will be excluded. Default is \code{NULL}: no cutoff
applied.}

\item{altitudeRange_AGL}{numeric vector of length 2 with start and end of the
altitude range in metres a.g.l. Echoes outside the altitude range will be
excluded. Default is \code{NULL}: no altitude filtering applied.}

\item{echoValidator}{logical. If set to \code{FALSE} (default), no additional
filter is applied; if set to \code{TRUE}, echoes labelled by the echo validator as
\code{"non-bio scatterer"} will be excluded.}

\item{outputDirPath}{Directory path where output files are saved. The output
filename is auto-generated from the active filter parameters. If \code{NULL}
(default), no files are written.}

\item{tagOutputFile}{Vector of two elements \code{c(prefix, suffix)} inserted
around the auto-generated filename. Either element may be \code{NULL}. Only used
when \code{outputDirPath} is not \code{NULL}.}

\item{saveCSV}{logical. If \code{TRUE} (default), creates a subdirectory named
after the auto-generated filename and writes the MR1-standard files:
\code{echoData.csv}, \code{protocolData.csv}, \code{blindTimesData.csv},
\code{sunriseSunsetData.csv}, \code{radarSiteData.csv}, \code{filterParameters.yaml},
\code{metaData.yaml}. Only used when \code{outputDirPath} is not \code{NULL}.}

\item{saveAsRDS}{logical. If \code{TRUE}, saves the full data list as a single
\code{.rds} file in \code{outputDirPath}. Useful as an in-session cache but not
required for the MR1 data standard. Default is \code{FALSE}.}
}
\value{
A named list with the following elements:
\describe{
\item{echoData}{Filtered echo data.}
\item{protocolData}{Filtered protocol data (columns defined by MR1 standard).}
\item{blindTimesData}{Filtered blind times data.}
\item{sunriseSunsetData}{Filtered sunrise/sunset data (columns defined by MR1 standard).}
\item{radarSiteData}{Radar and site metadata, with \code{targetTimeZone} added.}
\item{filterParameters}{Named list of the filter settings applied.}
\item{metaData}{Named list with per-table column metadata (name, type,
description), plus \code{database} name and \code{birdscanR} package version.}
}
}
\description{
\code{\link[=createDataPackage]{createDataPackage()}} compiles data from \code{\link[=extractDbData]{extractDbData()}} into
a standardized MR1 data package, applying filters on time range, pulse type,
rotation, echo class, altitude, and class probability. The output follows the
MR1 Data Standard (one package per campaign): \code{echoData}, \code{protocolData},
\code{blindTimesData}, \code{sunriseSunsetData}, \code{radarSiteData}, \code{filterParameters},
and \code{metaData}. Filtering is applied via \code{\link[=filterEchoData]{filterEchoData()}} and
\code{\link[=filterProtocolData]{filterProtocolData()}}. When \code{outputDirPath} is provided, the package is
saved by default as standardized CSV and YAML files suitable for Zenodo
deposit; saving as RDS requires \code{saveAsRDS = TRUE}.
}
\examples{
\donttest{
# Load example data
# ===========================================================================
dbData = readRDS(system.file("extdata",
  "CH_Sempach_2024_SEP24_25_DataExtract.rds",
  package = "birdscanR"
))
dbName = "CH_Sempach_2024_SEP24_25"
targetTimeZone = "Etc/GMT0"
timeRangeTargetTZ = c("2024-09-24 00:00", "2024-09-25 23:59")

# Set manual blind times to NULL (no manual blind times)
# ===========================================================================
cManualBlindTimes = NULL

# Create MR1 data package
# ===========================================================================
dataPackage = createDataPackage(
  echoData           = dbData$echoData,
  protocolData       = dbData$protocolData,
  blindTimesData     = cManualBlindTimes,
  sunriseSunsetData  = dbData$sunriseSunset,
  radarSiteData      = dbData$siteData,
  dbName             = dbName,
  timeRangeTargetTZ  = timeRangeTargetTZ,
  targetTimeZone     = targetTimeZone,
  pulseTypeSelection = "S",
  classSelection     = c("passerine_type"),
  outputDirPath      = getwd()
)
}
}
\seealso{
Other write file functions: 
\code{\link{saveMTR}()},
\code{\link{savePlotToFile}()}
}
\author{
Baptiste Schmid, Fabian Hertner, Birgen Haest
}
\concept{write file functions}
