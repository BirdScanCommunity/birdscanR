% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/addFeatSummToMTR.R
\name{addFeatSummToMTR}
\alias{addFeatSummToMTR}
\title{addFeatSummToMTR}
\usage{
addFeatSummToMTR(
  mtrDensVPTS,
  echoData,
  class = "allClasses",
  inputVariable,
  nCores = 2
)
}
\arguments{
\item{mtrDensVPTS}{dataframe with the MTR/Density/VPTS table created with
\code{\link[=computeMTR]{computeMTR()}}, \code{\link[=computeDensity]{computeDensity()}}, or \code{\link[=createVPTS]{createVPTS()}}.}

\item{echoData}{dataframe with the echo data from the data list created
with \code{\link[=extractDbData]{extractDbData()}} or \code{\link[=compileData]{compileData()}}.}

\item{class}{character string vector with the class(es) for which the
feature summary should be calculated. Default: \code{"allClasses"}, i.e. all
classes pooled together.}

\item{inputVariable}{Name of the feature, included in \code{echoData}, to
summarize.}

\item{nCores}{maximal number of cores used for parallelisation. Default
value is 2.}
}
\value{
\code{mtrDensVPTS}, with the added weighted summary statistics of
\code{inputVariable} for the requested \code{class}.
}
\description{
\code{\link[=addFeatSummToMTR]{addFeatSummToMTR()}} adds weighted summary statistics (n,
mean, SD) of a selected feature to a MTR/Density/VPTS table. Each
time-altitude bin of \code{mtrDensVPTS} is summarised from the matching echoes
in \code{echoData}, weighted by their MTR-factor (\code{mtr_factor_rf}). When
\code{inputVariable} is \code{"feature2.azimuth"}, circular statistics are used
instead of linear statistics.
}
\examples{
\donttest{
# Load example data
# ===========================================================================
dbData = readRDS(system.file("extdata",
  "CH_Sempach_2024_SEP24_25_DataExtract.rds",
  package = "birdscanR"
))
dbName = "CH_Sempach_2024_SEP24_25"
targetTimeZone = "Etc/GMT0"
timeRangeData = c("2024-09-24 00:00", "2024-09-25 23:59")

# Compute migration traffic rate
# ===========================================================================
classSelection.mtr = c("passerine_type")
mtrData = computeMTR(
  dbName = dbName,
  echoes = dbData$echoData,
  classSelection = classSelection.mtr,
  altitudeRange = c(25, 1025),
  altitudeBinSize = 50,
  timeRange = timeRangeData,
  timeBinDuration_sec = 1800,
  timeZone = targetTimeZone,
  sunriseSunset = dbData$sunriseSunset,
  sunOrCivil = "civil",
  crepuscule = "nauticalSolar",
  protocolData = dbData$protocolData,
  visibilityData = dbData$visibilityData,
  manualBlindTimes = NULL,
  saveBlindTimes = FALSE,
  blindTimesOutputDir = getwd(),
  blindTimeAsMtrZero = NULL,
  propObsTimeCutoff = 0,
  computePerDayNight = FALSE,
  computePerDayCrepusculeNight = FALSE,
  computeAltitudeDistribution = FALSE
)

# Add the weighted mean flight speed for the passerine_type class
# ===========================================================================
mtrData = addFeatSummToMTR(
  mtrDensVPTS = mtrData,
  echoData = dbData$echoData,
  class = "passerine_type",
  inputVariable = "feature37.speed",
  nCores = 1
)
}

}
\seealso{
Other manipulation functions:
\code{\link[=addDayNightInfoPerEcho]{addDayNightInfoPerEcho()}},
\code{\link[=computeDensity]{computeDensity()}},
\code{\link[=computeMTR]{computeMTR()}},
\code{\link[=convertTimeZone]{convertTimeZone()}},
\code{\link[=createVPTS]{createVPTS()}},
\code{\link[=filterSpeedFeature37]{filterSpeedFeature37()}},
\code{\link[=mergeVisibilityAndManualBlindTimes]{mergeVisibilityAndManualBlindTimes()}},
\code{\link[=reclassToBats]{reclassToBats()}},
\code{\link[=twilight]{twilight()}}
}
\author{
Birgen Haest, Baptiste Schmid
}
\concept{manipulation functions}
